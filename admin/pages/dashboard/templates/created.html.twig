{% if accounts is iterable %}
	<div data-id="created" class="sortable-handle col-md-6 col-lg-3">
		<div class="card card-info card-outline">
			<div class="card-header">
				<div class="row">
					<div class="col">
						<h5>Last 10 created</h5>
					</div>
					<div class="col">
						{{ include('_close_card_button.html.twig') }}
					</div>
				</div>
			</div>
			<div class="card-body p-0">
				<table class="table table-striped table-condensed">
					<thead>
					<tr>
						<th>#</th>
						<th>Account</th>
						<th>Creation Date</th>
					</tr>
					</thead>
					<tbody>
					{% set i = 0 %}
					{% for result in accounts %}
						{% set i = i + 1 %}
						<tr>
							<th>{{ i }}</th>
							<td><a href="?p=accounts&id={{ result.id }}">{{ result.id }}</a></td>
							<td>{{ result.created|date("M d Y, H:i:s") }}</td>
						</tr>
					{% endfor %}
					</tbody>
				</table>
			</div>
		</div>
	</div>
{% endif %}
