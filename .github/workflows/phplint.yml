name: PHP Linting
on:
  pull_request:
    branches: [develop]
  push:
    branches: [develop]

jobs:
  phplint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: overtrue/phplint@9.6
        with:
          path: .
          options: --exclude=*.log
